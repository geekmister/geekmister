# Blog Dev Commands

Manage the local Wagtail server from repository root:

```bash
make start
make stop
make restart
make status
make logs
```

Or use script directly:

```bash
./scripts/blog-server.sh start
./scripts/blog-server.sh stop
./scripts/blog-server.sh status
```

Default URL: http://127.0.0.1:8000
